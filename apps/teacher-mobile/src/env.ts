/**
 * Placeholder until the native project is wired to a real env-injection
 * mechanism (react-native-config or similar) — same gap as apps/mobile's
 * own env.ts. Fill these in from the same Supabase project as the web
 * app's VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY (see the root .env.example).
 */
export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
